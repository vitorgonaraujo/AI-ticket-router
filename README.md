# AI Ticket Router

Sistema automatizado de triagem de tickets de suporte utilizando a API do Cohere (ClientV2) para categorizar mensagens de clientes em tempo real.

## Requisitos

* Python 3.12+

## Como Executar

Para iniciar a triagem das mensagens, execute o seguinte comando no seu terminal:

```bash
poetry run python -m ai_ticket_router