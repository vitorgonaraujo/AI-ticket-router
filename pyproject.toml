[project]
name = "ai-ticket-router"
version = "0.1.0"
description = ""
authors = [
    {name = "vitorgonaraujo",email = "agente51411@gmail.com"}
]
readme = "README.md"
requires-python = ">3.12, <4.0"
dependencies = [
    "cohere (>=7.0.5,<8.0.0)",
    "python-dotenv (>=1.2.2,<2.0.0)"
]

[tool.poetry]
packages = [{include = "ai_ticket_router", from = "src"}]


[tool.poetry.group.dev.dependencies]
ipykernel = "^7.3.0"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
